micronaut:
  application:
    name: swagger-groovy
  server:
    port: 8087
    cors:
      enabled: true
  router:
    static-resources:
      swagger:
        paths: classpath:META-INF/swagger
        mapping: /swagger/**
